module vincent.display {
    export class LoadingBase extends SpriteBase {

        public constructor() {
            super();
        }

        protected init(): void {
        }

        public setProgress(current: number, total: number): void {
        }

        protected onRemoved() {
        }
    }
}
